{% if options is not empty %}
    <script>
        window.cookieconsent_options = {{ options|json_encode|raw }};
    </script>
{% endif %}

{% if script != false %}
    <script src="{{ script }}"></script>
{% endif %}
